- Redesign the video filter panel with one slider per row, integer snapping, a reset button per filter, and values applying once the drag ends
- Add filter presets: save the current adjustments as a named preset and apply or delete presets from both the player panel and the decoder settings page
- Replace per-filter toggles with a single master switch for all filters
- Align filter icons and value formats between the settings page and the player panel

---

- 重新设计视频滤镜面板，每个滤镜独占一行，滑杆按整数吸附，每个滤镜带独立重置按钮，拖动结束后才应用数值
- 新增滤镜预设：可将当前滤镜参数保存为命名预设，并在播放器面板和解码器设置页中快速应用或删除
- 用一个总开关取代每个滤镜的单独开关
- 统一设置页与播放器面板的滤镜图标和数值格式
